import { describe, it, expect } from "vitest";

// contoh tes sederhana untuk memastikan konfigurasi tes bekerja

describe("example", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});
